{
  "name": "judgement",
  "version": "1.0.0",
  "main": "dist/server.js", 
  "scripts": {
    "build": "tsc",
    "dev": "pm2 start ecosystem.config.js --only judgment-server-dev --env development",
    "start": "npm run build && pm2 start ecosystem.config.js --only judgment-server-prod --env production",
    "stop": "pm2 stop all",
    "delete": "pm2 delete all",
    "logs": "pm2 logs",
    "logs:json": "tail -F ../logs/combined.log | jq",
    "logs:error": "tail -F ../logs/error.log | jq", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "morgan": "^1.10.0",
    "socket.io": "^4.8.1",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/morgan": "^1.9.9",
    "@types/node": "^22.13.5",
    "nodemon": "^3.1.9",
    "pm2": "^6.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
